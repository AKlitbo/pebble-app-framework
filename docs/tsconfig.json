// typechecks the docs site tools and their specs
// they import marked from the docs package's own install, so a repo of faces never checks them
// run by npm run typecheck:docs and by the engine's CI after npm ci --prefix docs
{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "moduleDetection": "force",
    "allowImportingTsExtensions": true,
    "types": ["node"]
  },
  "include": ["tools/**/*.ts", "vitest.config.ts", "eslint.config.ts"]
}
