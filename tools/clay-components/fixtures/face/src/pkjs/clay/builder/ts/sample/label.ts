/**
 * A neighbour piece the fixture init imports, so the bundle has to follow a relative import.
 *
 * @param value The text to wrap into the sample label.
 * @return The sample label text.
 */
export function sampleLabel(value: string): string {
  return `sample ${value}`;
}
